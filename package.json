{
  "name": "figma-svg-import",
  "version": "1.0.0",
  "description": "URL To Vector - Import SVG without Downloads",
  "scripts": {
    "build": "esbuild code.ts --bundle --outfile=code.js --sourcemap --external:react --external:react-dom --format=iife",
    "watch": "esbuild code.ts --bundle --outfile=code.js --sourcemap --external:react --external:react-dom --format=iife --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "1.58.0",
    "esbuild": "0.17.19"
  }
}
